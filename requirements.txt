# Local (analysis only — no torch, this machine has ~3GB free)
numpy>=1.24
pandas>=2.0
pydicom>=2.4
tqdm>=4.66

# Training environments (Kaggle/Colab already provide these; pinned here for reference)
# torch>=2.4
# timm>=1.0
# scikit-learn>=1.4
# transformers>=4.44
